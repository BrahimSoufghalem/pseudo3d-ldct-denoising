torch>=2.0.0
torchvision>=0.15.0
torchmetrics>=1.0.0
monai>=1.3.0
pydicom>=2.4.0,<3.0.0
scikit-learn>=1.3.0
scikit-image>=0.21.0
tqdm>=4.65.0
tensorboard>=2.14.0
pandas>=2.0.0
requests>=2.31.0
urllib3>=2.0.0
nbiatoolkit>=1.0.0
einops>=0.7.0
wandb

# ---------------------------------------------------------------------------
# OPTIONAL (strongly recommended on CUDA): official Mamba selective-scan kernel.
# Without it the model still runs, but falls back to a pure-PyTorch scan that is
# orders of magnitude slower (fine for CPU tests, impractical for training).
#
#   pip install ninja
#   pip install causal-conv1d>=1.4.0 --no-build-isolation
#   pip install mamba-ssm --no-build-isolation
# ---------------------------------------------------------------------------
